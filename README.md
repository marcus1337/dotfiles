# dotfiles

Personal dotfiles for Arch Linux and related setups.

## Structure

- `tmux/` — tmux config
- `nvim/` — Neovim config
- `zsh/` — Zsh config
- `vim/` — Vim config
- `install/archlinux/` — setup scripts for Arch Linux

## Setup

```sh
./install/archlinux/setup.sh
```
