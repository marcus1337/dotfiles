
source-file ~/.config/tmux/basics.conf
source-file ~/.config/tmux/window.conf

# ===== Session / window quality-of-life =====

# Detach quickly
bind d detach

# Kill current pane/window confirmations are fine; leave defaults

source-file ~/.config/tmux/plugins.conf
source-file ~/.config/tmux/visuals.conf

